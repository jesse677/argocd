apiVersion: batch/v1
kind: CronJob
metadata:
  name: civic-radar-crime
  namespace: civic-radar
spec:
  schedule: "15 6 * * *"
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      backoffLimit: 1
      activeDeadlineSeconds: 900
      template:
        spec:
          restartPolicy: Never
          containers:
          - name: crime
            image: ghcr.io/jesse677/civic-radar:latest
            command: ["python", "run_crime.py"]
            envFrom:
            - configMapRef:
                name: civic-radar-config
            env:
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: civic-radar-secrets
                  key: pg-password
            resources:
              requests:
                cpu: 100m
                memory: 256Mi
              limits:
                cpu: 500m
                memory: 512Mi
