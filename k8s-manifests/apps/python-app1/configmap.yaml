apiVersion: v1
kind: ConfigMap
metadata:
  name: python-app1-config
  namespace: python-app1
data:
  # Add your application configuration here
  app.conf: |
    # Example configuration
    debug = false
    log_level = info
