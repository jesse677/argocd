apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tina-paper-data
  namespace: tina-paper
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi
